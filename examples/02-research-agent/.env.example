ANTHROPIC_API_KEY=
TENAME_DATABASE_URL=postgresql+psycopg://tename:tename@localhost:5433/tename_dev

# Passphrase used by the Vault to encrypt credentials at rest.
# Pick something long; losing this password means losing access to
# every credential you've stored.
TENAME_VAULT_PASSPHRASE=
