# Required — your Anthropic API key.
ANTHROPIC_API_KEY=

# Optional — override the default Postgres URL if you're not using the
# repo's docker compose stack.
# TENAME_DATABASE_URL=postgresql+psycopg://tename:tename@localhost:5433/tename_dev
