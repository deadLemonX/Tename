"""Bundled Tename profiles.

YAML files in this package ship with the wheel and are loaded via
`importlib.resources`. Users can add their own profiles on disk and point
`ProfileLoader` at them via the `search_paths` argument.
"""
